version: '3.8'

services:
  app:
    build: .
    container_name: ai-votes
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./config:/app/config
    environment:
      - PORT=8000
      - DATABASE_URL=sqlite:///./data/database.db
    restart: unless-stopped
